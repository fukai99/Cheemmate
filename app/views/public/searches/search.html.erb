<h2>Results index</h2>
<!--検索対象モデルがUserの時 -->
  <% @range == "User" %>
    
      <% @users.each do |user| %>
        <%= image_tag user.get_avatar(70,70) %><br>
          <%= user.disply_name %>
          <%= user.is_member ? '生徒' : '家族' %>
      <% end %>
    
  
